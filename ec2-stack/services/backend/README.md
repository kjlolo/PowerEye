# Backend API

FastAPI control plane API for:
- JWT login and RBAC (admin/viewer)
- Site metadata CRUD
- Fleet overview/latest/timeseries
- OTA firmware releases, jobs, check, and report

The service auto-creates schema and seeds first admin on startup.
